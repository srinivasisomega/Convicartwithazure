@model ConvicartWebApp.PresentationLayer.ViewModels.SubscriptionViewModel

<style>
    .active-subscription {
        position: relative; /* Ensures positioning for the pseudo-element */
        border: 7px solid #008000;
        box-shadow: 0 0 10px rgba(0, 123, 255, 0.5);
        padding: 16px; /* Add some padding to give space for the tick mark */
        border-radius: 8px; /* Optional: smoothens the corners */
    }

        .active-subscription::after {
            content: "✔Current"; /* Unicode for checkmark */
            font-size: 24px;
            color: #007BFF; /* Same color as the border */
            position: absolute;
            top: 8px; /* Position the tick mark at the top-right corner */
            right: 8px;
        }

    .Sub-title {
        display: flex;
        flex-direction: column;
    }

    .Subscription ul {
        height: 144px;
    }

    .Subscription {
        width: 404px;
        height: 463px;
        margin-left: 30px;
        padding-left: 20px;
        padding-top: 10px;
        display: flex;
        flex-direction: column;
        justify-content: space-evenly;
        align-items: flex-start;
    }

    .Bronze {
        background-color: #CD7F32;
    }

    .Silver {
        background-color: #C0C0C0;
    }

    .Gold {
        background-color: #FFD700;
    }

    .amount {
        display: flex;
        flex-direction: row;
        align-items: flex-end;
    }

        .amount h2 {
            font-size: 48px;
            font-weight: bold;
        }

        .amount h5 {
            font-size: 20px;
            margin-bottom: 16px;
        }
</style>
<!-- Bronze Subscription -->
<!-- Bronze Subscription -->
<form asp-action="UpdateSubscription" method="post" class="Subscription Bronze @(Model.CurrentSubscriptionType == "Bronze" && Model.CurrentSubscriptionDuration == "Month" ? "active-subscription" : "")">

    <input type="hidden" name="customerId" value="@Model.CustomerId" />
    <input type="hidden" name="subscriptionType" value="Bronze" />
    <input type="hidden" name="days" value="30" />
    <input type="hidden" name="amount" value="5000" />
    <div class="Sub-title">
        <h3>Bronze</h3>
        <div class="amount">
            <h2>₹ 5000</h2>
            <h5>/Month</h5>
        </div>
    </div>
    <ul>
        <li>Order ingredients for one person.</li>
        <li>Get access to basic recipes.</li>
        <li>Get info about calories consumed.</li>
    </ul>
    <button type="submit" class="btn btn-primary btn-block">Purchase</button>
</form>

<!-- Silver Subscription -->
<form asp-action="UpdateSubscription" method="post" class="Subscription Silver @(Model.CurrentSubscriptionType == "Silver" && Model.CurrentSubscriptionDuration == "Month" ? "active-subscription" : "")">
    <input type="hidden" name="customerId" value="@Model.CustomerId" />
    <input type="hidden" name="subscriptionType" value="Silver" />
    <input type="hidden" name="days" value="30" />
    <input type="hidden" name="amount" value="8000" />
    <div class="Sub-title">
        <h3>Silver</h3>
        <div class="amount">
            <h2>₹ 8000</h2>
            <h5>/Month</h5>
        </div>
    </div>
    <ul>
        <li>Order ingredients for two people.</li>
        <li>Get access to all recipes.</li>
        <li>Get complete nutritional info about the meals made using the app.</li>
    </ul>
    <button type="submit" class="btn btn-primary btn-block">Purchase</button>
</form>

<!-- Gold Subscription -->
<form asp-action="UpdateSubscription" method="post" class="Subscription Gold @(Model.CurrentSubscriptionType == "Gold" && Model.CurrentSubscriptionDuration == "Month" ? "active-subscription" : "")">
    <input type="hidden" name="customerId" value="@Model.CustomerId" />
    <input type="hidden" name="subscriptionType" value="Gold" />
    <input type="hidden" name="days" value="30" />
    <input type="hidden" name="amount" value="10000" />
    <div class="Sub-title">
        <h3>Gold</h3>
        <div class="amount">
            <h2>₹ 10000</h2>
            <h5>/Month</h5>
        </div>
    </div>
    <ul>
        <li>Order ingredients for up to 4 people.</li>
        <li>Get access to all recipes.</li>
        <li>Get complete analysis of nutrients based on the meals consumed by the user.</li>
        <li>Get suggested recipes to get nutrients neglected in your other meals.</li>
    </ul>
    <button type="submit" class="btn btn-primary btn-block">Purchase</button>
</form>
